language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t cocoacasts/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cocoacasts/docker-react npm run test
